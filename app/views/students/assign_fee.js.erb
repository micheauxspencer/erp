console.log($("#form_" + '<%= "#{@student_id}_#{@fee_id}" %>' + " input[type=submit]"));
console.log('<%= "#{@student_id}_#{@fee_id}" %>');

$("#form_" + '<%= "#{@student_id}_#{@fee_id}" %>' + " input[type=submit]").hide(200);
