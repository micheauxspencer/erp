<h3 >Select Term</h3>
<%= select_tag "term_id", options_from_collection_for_select(Term.all, "id", "name", @term_id ), class: "form-control" %>
<button class="btn btn-primary select-term" >Select</button>

<%= render :partial => 'form_enter_mark' %>

<script type="text/javascript">
  $(".select-term").click(function(){
    var term_id = $('#term_id').val();
    var url = "<%= select_term_path(@student) %>";
    $.ajax({
      url: url,
      type: "get",
      dataType: "json",
      data: "term_id=" + term_id,
      asycn: true,
      success: function(data) {
        
      },
      error: function(err){
        // alert("Error")
      }
    });
  });
</script>


<style type="text/css">
#enter_mark {
  max-width: 900px;
  margin: 0 auto;
  margin-top: 20px;
  margin-bottom: 20px;
}
.table-magin-top {
  margin-top: 5px;
}
.padding-td {
  padding-left: 5px;
}
.input-mark {
  width: 100%;
  text-align: right;
}
.save-mark {
  margin-top: 20px;
}
#term_id {
  width: 200px;
  display: inline-block;
}
.select-mark {
  width: 100%;
}
.border {
  border: 1px solid #000;
}
.border_no_top {
  border-left: 1px solid #000;
  border-right: 1px solid #000;
  border-bottom: 1px solid #000;
}
</style>